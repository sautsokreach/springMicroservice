spring:
  datasource:
    url: jdbc:postgresql://user-db:5432/user_service
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    root: WARN
    com.microservice: INFO
